"""export —— 将 Markdown 导出为 HTML(成员4 模块)。

待实现,见 Issue: export 子命令。
预期功能:
  - 读取 Markdown,转换为 HTML(可用标准库或轻量实现)
  - 套一个最小 HTML 模板(含 <head>/<meta charset>)
  - -o 指定输出路径,默认同名 .html
"""


def run(args) -> int:
    raise NotImplementedError(
        "export 尚未实现。负责人:成员4。参见仓库 Issue。"
    )
